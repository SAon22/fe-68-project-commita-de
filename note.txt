fix path api ...
    maybe can fix can not log in problem

maybe fix style first page

log-in with user and admin to compare different and compare with no register

create massageShop file for user to see all massageShop

fix cover in first page to full screen or maybe change it to full screen then decorate more around

design UI flow in figma
    draw each page in my Ipad in goodnotes

something about backend idk it should in this file

...and many more ...


CheckList folder from a10:
    -app x
    -components x
    -libs
        - ตอนนี้ใช้ localhost fetch อยู่ ค่อยแก้เป็น backend ที่ deploy แล้วทีหลัง
    -redux
        - /features/reserveSlice/ts อาจจะเปลี่ยนพวก fields ที่ส่งเข้า น่าจะ date 

api authOptions มั่วอยู่ก้อปผิด repo 55555555555

add image for each massageShop ทำใน backend ? หรือ item สักที่

เน้นทำเหมือนๆกับ assignment หรือ workshop ไปก่อน ค่อยไปปรับเปลี่ยนให้สวยทีหลัง

app/layout.tsx
import NextAuthProvider from "@/providers/NextAuthProvider"; ยังแดงอยู่

ให้ resigter ไปอยู่หน้า sign in
ให้แสดงชื่อ user ตอน admin ดู all reservation
อาจจะแก้ ย้ายบางอันไปเป็น components เช่น register to components/registerform เพื่อให้เรียบร้อย
NOTE:
    6 account user1 2 3, admin1 2 3 : password: 12345678
    5 massageShop AAAAAA B C D E 